{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portfolio statique auto-alimente depuis GitHub et PocketBase",
  "scripts": {
    "collect": "node src/collect.mjs",
    "build": "node src/build.mjs",
    "dev": "node src/build.mjs && node src/serve.mjs",
    "all": "npm run collect && npm run build",
    "health": "node src/health.mjs"
  },
  "dependencies": {
    "marked": "^14.1.4",
    "yaml": "^2.6.1"
  }
}
